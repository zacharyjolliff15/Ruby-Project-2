<div style="max-width: 600px;">
  <div style="margin-bottom: 20px;">
    <%= link_to "← Back to #{@animal.name}", animal_path(@animal), style: "color: #3498db; text-decoration: none;" %>
  </div>
  
  <h1 style="color: #2c3e50; margin-bottom: 30px;">Edit <%= @animal.name %></h1>

  <%= render "form", animal: @animal %>
</div>